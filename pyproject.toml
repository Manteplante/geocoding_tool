[project]
name = "geocoding-tool"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Manteplante", email = "matiasmantepizarro@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "geopandas>=1.1.4",
    "pandas>=3.0.5",
]

[project.scripts]
geocoding-tool = "geocoding_tool:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"
